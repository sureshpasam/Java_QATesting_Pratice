pipeline {
  agent {
    docker { image 'node:16-alpine' 
             maven 'maven-3.6.3'}
  }
  stages {
    stage('Build') {
      steps {
        sh 'mvn clean package'
      }
    stage('Bui') {
      steps {
        sh 'node --version'
      }
    }
  }
}
